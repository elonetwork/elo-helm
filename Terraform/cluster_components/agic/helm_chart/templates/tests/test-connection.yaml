apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agic.fullname" . }}-test-connection"
  labels:
    {{- include "agic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
