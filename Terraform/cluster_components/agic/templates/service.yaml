apiVersion: v1
kind: Service
metadata:
  namespace: devops-tools
  name: jenkins-service
spec:
  selector:
    app: jenkins-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
