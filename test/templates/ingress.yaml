apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
    appgw.ingress.kubernetes.io/override-frontend-port: "333"
spec:
  rules:
  - host: "www.jenkins.sisbaanas.com"
    http:
      paths:
      - path: /test/
        pathType: Prefix
        backend:
          service:
            name: test-service
            port:
              number: 8080